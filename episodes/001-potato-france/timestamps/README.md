# Timestamp status

Actual word-level timing data was not preserved. `word-timestamps.csv` is intentionally empty and must not be populated from estimates. Generate it from the approved voice track if that track becomes available.
